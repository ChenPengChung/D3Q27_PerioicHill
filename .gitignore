# ========================================
# GitHub 上傳設定 (保留所有程式碼檔案)
# ========================================

# 排除模擬結果目錄中的大檔案，但保留腳本和媒體檔案
result/*.vtk
result/*.bin
result/*.dat
result/*.DAT
result/*.raw
result/*.pvd
result/*.vti
result/*.vtu
result/*.vtp
result/*.pvtu
result/*.pvtp
backup/
statistics/

# 排除資料檔案
*.dat
*.DAT

# 排除編譯產物
a.out
*.o
*.exe

# 排除運行 log 檔案 (根目錄)
/log*

# 排除暫存檔案
*~
*.tmp
*.swp

# 排除 IDE 設定檔 (保留 .vscode 目錄)
.vs/

# 排除壓縮檔
*.rar
*.zip

# ========================================
# 說明 (會上傳 GitHub)
# ========================================
# - 會上傳: .cu, .h, .c 程式碼檔案
# - 會上傳: initial_D3Q19/*.c 程式碼檔案
# - 會上傳: .vscode/ 目錄（包括所有設定檔、日誌）
# - 會上傳: .gitignore
# - 會上傳: *.md 文件
# - 會上傳: result/*.py  Python 腳本
# - 會上傳: result/*.png *.jpg *.gif *.mp4 圖片影片
# - 不上傳: result/*.vtk *.bin *.dat 等大型資料檔
# ========================================
._*
*.tar.gz

result/*.avi
